; Generated by slimec (stage 2)
; Target: windows-x64

default rel

section .data
str0: db "Fib test", 0
str0_len equ $ - str0 - 1
str1: db 10, 0
str1_len equ $ - str1 - 1
str2: db "Result:", 0
str2_len equ $ - str2 - 1
str3: db " ", 0
str3_len equ $ - str3 - 1

section .bss
_global_result: resq 1
_itoa_buf: resb 24

section .text
global main
extern GetStdHandle
extern WriteFile
extern ExitProcess

main:
    push rbp
    mov rbp, rsp
    sub rsp, 2048
    and rsp, -16
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    lea rdx, [str0]
    mov r8, str0_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 10
    mov rdi, rax
    call fib
    mov [_global_result], rax
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    lea rdx, [str2]
    mov r8, str2_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    lea rdx, [str3]
    mov r8, str3_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, [_global_result]
    call _itoa
    push rdi
    push rax
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    pop r8
    pop rdx
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, -11
    call GetStdHandle
    mov rcx, rax
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, 0
    call ExitProcess

; Helper: int to string (rax = number, returns rdi = buffer, rax = length)
_itoa:
    push rbx
    push rcx
    push rdx
    mov rcx, _itoa_buf + 20
    mov rbx, 10
    xor r8, r8
    cmp rax, 0
    jge .positive
    neg rax
    mov r8, 1
.positive:
.loop:
    xor rdx, rdx
    div rbx
    add dl, '0'
    dec rcx
    mov [rcx], dl
    test rax, rax
    jnz .loop
    cmp r8, 1
    jne .done
    dec rcx
    mov byte [rcx], '-'
.done:
    mov rdi, rcx
    mov rax, _itoa_buf + 20
    sub rax, rcx
    pop rdx
    pop rcx
    pop rbx
    ret

fib:
    push rbp
    mov rbp, rsp
    sub rsp, 256
    mov [rbp-8], rdi
    mov rax, 1
    push rax
    mov rax, [rbp-8]
    pop rbx
    cmp rax, rbx
    setle al
    movzx rax, al
    test rax, rax
    jz .L1
    mov rax, [rbp-8]
    leave
    ret
    jmp .L0
.L1:
.L0:
    mov rax, 2
    push rax
    mov rax, [rbp-8]
    pop rbx
    sub rax, rbx
    mov rdi, rax
    call fib
    push rax
    mov rax, 1
    push rax
    mov rax, [rbp-8]
    pop rbx
    sub rax, rbx
    mov rdi, rax
    call fib
    pop rbx
    add rax, rbx
    leave
    ret
    xor rax, rax
    leave
    ret
