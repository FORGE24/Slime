; Generated by slimec (stage 2)
; Target: windows-x64

default rel

section .data
str0: db "=== Preparation Phase Demo ===", 0
str0_len equ $ - str0 - 1
str1: db 10, 0
str1_len equ $ - str1 - 1
str2: db "Test 1 - Constant Folding Chain:", 0
str2_len equ $ - str2 - 1
str3: db "Test 2 - Nested Computation:", 0
str3_len equ $ - str3 - 1
str4: db "Test 3 - Complex Expression:", 0
str4_len equ $ - str4 - 1
str5: db "=== All computed at Preparation Time! ===", 0
str5_len equ $ - str5 - 1

section .bss
_cached_stdout: resq 1  ; I/O Optimization: cached stdout handle\n_global_val1: resq 1
_global_val2: resq 1
_global_val3: resq 1
_global_val4: resq 1
_global_val5: resq 1
_global_val6: resq 1
_global_x: resq 1
_global_y: resq 1
_global_z: resq 1
_global_result1: resq 1
_global_result2: resq 1
_global_result3: resq 1
_global_final_result: resq 1
_global_p: resq 1
_global_q: resq 1
_global_r: resq 1
_global_s: resq 1
_itoa_buf: resb 24

section .text
global main
extern GetStdHandle
extern WriteFile
extern ExitProcess

main:
    push rbp
    mov rbp, rsp
    sub rsp, 2048
    and rsp, -16

    ; I/O Optimization: Cache stdout handle (9th optimization)
    mov rcx, -11
    call GetStdHandle
    mov [_cached_stdout], rax

    mov rcx, [_cached_stdout]
    lea rdx, [str0]
    mov r8, str0_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 100
    mov [_global_val1], rax
    mov rax, 200
    mov [_global_val2], rax
    mov rax, 300
    mov [_global_val3], rax
    mov rax, 600
    mov [_global_val4], rax
    mov rax, 500
    mov [_global_val5], rax
    mov rax, 250
    mov [_global_val6], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str2]
    mov r8, str2_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 250
    mov rdi, rax
    call printint
    mov rax, 5
    mov [_global_x], rax
    mov rax, 10
    mov [_global_y], rax
    mov rax, 15
    mov [_global_z], rax
    mov rax, 15
    mov [_global_result1], rax
    mov rax, 30
    mov [_global_result2], rax
    mov rax, 60
    mov [_global_result3], rax
    mov rax, 55
    mov [_global_final_result], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str3]
    mov r8, str3_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 55
    mov rdi, rax
    call printint
    mov rax, 7
    mov [_global_p], rax
    mov rax, 3
    mov [_global_q], rax
    mov rax, 25
    mov [_global_r], rax
    mov rax, 625
    mov [_global_s], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str4]
    mov r8, str4_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 625
    mov rdi, rax
    call printint
    mov rcx, [_cached_stdout]
    lea rdx, [str5]
    mov r8, str5_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, 0
    call ExitProcess

; Helper: int to string (rax = number, returns rdi = buffer, rax = length)
_itoa:
    push rbx
    push rcx
    push rdx
    mov rcx, _itoa_buf + 20
    mov rbx, 10
    xor r8, r8
    cmp rax, 0
    jge .positive
    neg rax
    mov r8, 1
.positive:
.loop:
    xor rdx, rdx
    div rbx
    add dl, '0'
    dec rcx
    mov [rcx], dl
    test rax, rax
    jnz .loop
    cmp r8, 1
    jne .done
    dec rcx
    mov byte [rcx], '-'
.done:
    mov rdi, rcx
    mov rax, _itoa_buf + 20
    sub rax, rcx
    pop rdx
    pop rcx
    pop rbx
    ret

small_add:
    push rbp
    mov rbp, rsp
    sub rsp, 256
    mov [rbp-8], rdi
    mov [rbp-16], rsi
    mov rax, [rbp-16]
    push rax
    mov rax, [rbp-8]
    pop rbx
    add rax, rbx
    leave
    ret
    xor rax, rax
    leave
    ret

compute_result:
    push rbp
    mov rbp, rsp
    sub rsp, 256
    mov rax, 10
    mov [rbp-8], rax
    mov rax, 20
    mov [rbp-16], rax
    mov rax, 30
    mov [rbp-24], rax
    mov rax, [rbp-24]
    push rax
    mov rax, [rbp-16]
    push rax
    mov rax, [rbp-8]
    pop rbx
    add rax, rbx
    pop rbx
    add rax, rbx
    mov [rbp-32], rax
    mov rax, 2
    push rax
    mov rax, [rbp-32]
    pop rbx
    imul rax, rbx
    mov [rbp-40], rax
    mov rax, [rbp-40]
    leave
    ret
    xor rax, rax
    leave
    ret
