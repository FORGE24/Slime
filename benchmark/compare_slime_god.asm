; Generated by slimec (stage 2)
; Target: windows-x64

default rel

section .data
str0: db "=== Slime God Mode (å¸¸éæå ä¼å) ===", 0
str0_len equ $ - str0 - 1
str1: db 10, 0
str1_len equ $ - str1 - 1
str2: db "ã1ãFibonacci: ", 0
str2_len equ $ - str2 - 1
str3: db "ã2ãSum: ", 0
str3_len equ $ - str3 - 1
str4: db "ã3ãCompute: ", 0
str4_len equ $ - str4 - 1
str5: db "ã4ãChain: ", 0
str5_len equ $ - str5 - 1
str6: db "ã5ãComplex: ", 0
str6_len equ $ - str6 - 1
str7: db "=== God Mode: 100% å¸¸éæå  ===", 0
str7_len equ $ - str7 - 1

section .bss
_cached_stdout: resq 1  ; I/O Optimization: cached stdout handle\n_global_fib30: resq 1
_global_sum_million: resq 1
_global_val1: resq 1
_global_val2: resq 1
_global_val3: resq 1
_global_val4: resq 1
_global_a: resq 1
_global_b: resq 1
_global_c: resq 1
_global_d: resq 1
_global_e: resq 1
_global_x: resq 1
_global_y: resq 1
_global_z: resq 1
_itoa_buf: resb 24

section .text
global main
extern GetStdHandle
extern WriteFile
extern ExitProcess

main:
    push rbp
    mov rbp, rsp
    sub rsp, 2048
    and rsp, -16

    ; I/O Optimization: Cache stdout handle (9th optimization)
    mov rcx, -11
    call GetStdHandle
    mov [_cached_stdout], rax

    mov rcx, [_cached_stdout]
    lea rdx, [str0]
    mov r8, str0_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 832040
    mov [_global_fib30], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str2]
    mov r8, str2_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 832040
    mov rdi, rax
    call printint
    mov rax, 499999500000
    mov [_global_sum_million], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str3]
    mov r8, str3_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 499999500000
    mov rdi, rax
    call printint
    mov rax, 100
    mov [_global_val1], rax
    mov rax, 200
    mov [_global_val2], rax
    mov rax, 300
    mov [_global_val3], rax
    mov rax, 600
    mov [_global_val4], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str4]
    mov r8, str4_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 600
    mov rdi, rax
    call printint
    mov rax, 10
    mov [_global_a], rax
    mov rax, 20
    mov [_global_b], rax
    mov rax, 30
    mov [_global_c], rax
    mov rax, 900
    mov [_global_d], rax
    mov rax, 300
    mov [_global_e], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str5]
    mov r8, str5_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, 300
    mov rdi, rax
    call printint
    mov rax, 5
    mov [_global_x], rax
    mov rax, 10
    mov [_global_y], rax
    mov rax, -25
    mov [_global_z], rax
    mov rcx, [_cached_stdout]
    lea rdx, [str6]
    mov r8, str6_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rax, -25
    mov rdi, rax
    call printint
    mov rcx, [_cached_stdout]
    lea rdx, [str7]
    mov r8, str7_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, [_cached_stdout]
    lea rdx, [str1]
    mov r8, str1_len
    lea r9, [rbp-200]
    mov qword [rsp+32], 0
    call WriteFile
    mov rcx, 0
    call ExitProcess

; Helper: int to string (rax = number, returns rdi = buffer, rax = length)
_itoa:
    push rbx
    push rcx
    push rdx
    mov rcx, _itoa_buf + 20
    mov rbx, 10
    xor r8, r8
    cmp rax, 0
    jge .positive
    neg rax
    mov r8, 1
.positive:
.loop:
    xor rdx, rdx
    div rbx
    add dl, '0'
    dec rcx
    mov [rcx], dl
    test rax, rax
    jnz .loop
    cmp r8, 1
    jne .done
    dec rcx
    mov byte [rcx], '-'
.done:
    mov rdi, rcx
    mov rax, _itoa_buf + 20
    sub rax, rcx
    pop rdx
    pop rcx
    pop rbx
    ret
