// slime 所有权系统测试

// 1. 基本所有权
let x = 10
call host.stdout x

// 2. 可变变量
var y = 20
y = y + 5
call host.stdout y

// 3. 正常使用
let a = 100
let b = a + 50
call host.stdout b

// 4. 作用域测试
if true
    let inner = 999
    call host.stdout inner
end

call host.stdout "所有权测试通过"
