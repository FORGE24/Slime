// slime 借用系统测试

// 1. 基本值和借用
let x = 42
let ref_x = &x        // 不可变借用
call host.stdout x
call host.stdout *ref_x   // 解引用

// 2. 可变借用
var y = 100
let ref_y = &mut y    // 可变借用
call host.stdout y

// 3. 通过引用修改 (需要实现)
// *ref_y = 200

call host.stdout "借用测试完成"
